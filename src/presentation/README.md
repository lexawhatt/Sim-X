# Presentation

Presentation converts domain read models into visuals and user gestures into
typed application intents.

It may read public domain models and foundation types. Domains never import it.
It does not implement scientific formulas or mutate canonical state directly.

