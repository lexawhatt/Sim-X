# Sim;math

This directory owns functions, geometry, coordinate systems, curves, and
stereometry.

It may expose renderer-neutral 2D or 3D read models, but it must not store
Sim;Engine scenes, cameras, meshes, colors, or GPU resources as mathematical
state. Retained 3D rendering is selected by the presentation adapter.

